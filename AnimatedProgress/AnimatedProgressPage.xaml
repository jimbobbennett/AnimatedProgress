<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
	   		 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:local="clr-namespace:AnimatedProgress"
			 x:Class="AnimatedProgress.AnimatedProgressPage">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<ProgressBar Grid.Row="1" Margin="20,0" 
		 			 local:AttachedProperties.AnimatedProgress="{Binding Progress}"/>

		<Entry Grid.Row="2" Margin="20,0" Text="{Binding ProgressPercent}"
		       VerticalOptions="Center"/>
	</Grid>
</ContentPage>
